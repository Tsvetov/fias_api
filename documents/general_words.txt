Сервис состоит из двух частей:

1. По таймауту скачивает базу с фаис, разварачивает в базу postgres, сравнивает с существующей, 
если есть различия. То заменяет существующую на новую(TODO: найти только отличия и заменить, без перезаписи в базу).

2. web сервис, который реализует веб апи.
Передача происходит в формате json.

